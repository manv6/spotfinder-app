{
  "name": "spotfinder-app",
  "version": "1.0.0",
  "private": true,
  "description": "Build entrypoint for deploy platforms that build from the repo root. The app itself lives in mobile/.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "cd mobile && npm ci && npm run build",
    "start": "node mobile/server.js"
  }
}
